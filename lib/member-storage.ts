// Shared with PremiumGate - the confirmed-unlocked member's email, so other
// client components (like the AI Assistant) can identify the member on
// their own API calls without re-running the unlock flow.
export const MEMBER_EMAIL_STORAGE_KEY = "quickfpl-member-email";
